# Root Environment Variables
# Copy this file to .env and update with your values

# Database
# Docker PostgreSQL runs on port 5433 (to avoid conflict with local PostgreSQL on 5432)
DATABASE_URL=postgresql://cicloazul:cicloazul123@localhost:5433/cicloazul

# Application
NODE_ENV=development

# PRODUCTION ENVIRONMENT VARIABLES
# Este arquivo é usado no build de produção
# Substitua YOUR_RAILWAY_URL pela URL real do seu backend

# API Configuration
API_URL=https://ciclo-azulbackend-production.up.railway.app/api
NODE_ENV=production

# IMPORTANTE: NÃO coloque secrets do backend aqui!
# O mobile não precisa de JWT_SECRET, DB_PASSWORD, etc.
# Essas variáveis são APENAS do backend.
